#include <iostream>

using namespace std;

int main()
{
    cout<<"Enter a and B ";
    int a,b,max,min;
   cout << "A + B = " << a + b << '\n'
     << "A - B = " << a - b << '\n'
     << "A * B = " << a * b << '\n'
     << "A / B = " << a / b << '\n';
     max=a*b;

    cout<<"max="<<a*b<< '\n';
        
    return 0;
}
